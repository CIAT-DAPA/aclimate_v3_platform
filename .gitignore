# Environment
.env
!.env.example

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Docker
**/.next/
**/node_modules/

# Database dumps (backups are not tracked in git)
bootstrap/db/dumps/
*.dump
*.backup
*.sql.gz

# Keycloak realm with real secrets (use aclimate-realm.example.json)
config/keycloak/aclimate-realm.json

# GeoServer data_dir (heavy raster data, not tracked in git)
bootstrap/geoserver/data_dir/*
!bootstrap/geoserver/data_dir/.gitkeep
