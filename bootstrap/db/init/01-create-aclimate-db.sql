-- The main database is created by the POSTGRES_DB env variable.
-- This script runs only if the volume is empty (first start).
-- Add any additional initialization SQL here if needed.

-- Example: Create extensions required by the platform
-- CREATE EXTENSION IF NOT EXISTS postgis;
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";